import React, {FC} from 'react';
import styled from 'styled-components/native';


const Calendar = () => {
  return <StyledText>달력 페이지</StyledText>;
};

const StyledText = styled.Text`
  font-size: 30px;
`;

export default Calendar;